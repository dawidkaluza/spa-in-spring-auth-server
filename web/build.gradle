plugins {
    id "com.github.node-gradle.node" version "5.0.0"
}

group = 'pl.dkaluza'
version = 'unspecified'

node {
    download = true
    version = "18.16.1"
}

tasks.register('npmBuild', NpmTask) {
    args = [ 'run', 'build' ]
}

